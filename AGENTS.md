# 项目工作纪律

- 最新代号和实施授权见MEMORY.md：旧版本称“瓦demo”，新增历史库与好友真人PvP；另做“新demo”，采用新策划并保持入口、存档隔离。用户已授权本轮实施，旧文档的“仅讨论”不能作为停止实施的理由。所有主要实现及返修交给DeepSeek，主代理验收。

- 当前卡牌只显示行动点费用，不用金、银、铜、黑铁等词给选手划档次；费用是玩法参数，不代表对真实选手能力的评价。

- 用户要求主动让 DeepSeek 分担大部分适合独立处理的实现初稿、资料整理、文案、测试与代码审查，复杂任务优先使用官方接口当前可用的高级模型。主代理负责拆分、审核、集成和验收，不得把未经执行或验证的输出称为已完成。
- 优先把任务主体和最消耗 token 的部分交给 DeepSeek，包括长篇方案推演、批量内容、完整模块初稿和返修。主代理避免重复从头撰写这些交付物，重点提出具体问题并验收；发现初稿不合格时先给明确返修要求。以实际完成的委派为准，不用调用次数充数。
- 调用设置有限预算和超时，记录结果到被 Git 忽略的 reports/。通过环境变量提供用户指定的本地密钥文件路径，不打印、复制或提交密钥。网页与线上服务器不得调用模型；部署不需要模型密钥。
- 修改 ui-source.js、规则模块、base-style.css 或 season-ui.css 后执行 npm run build。app.js / style.css 是生成文件。交付前执行 npm test。
- 优先做好《登峰赛季》游戏本体，保留卡框、手牌、战斗场地和独立地图，不扩张账号、League 或商业化系统。
- 机制优先参考《杀戮尖塔》一代，核实事实后再适配。项目自行调整的概率、费用与稀有度关系、重复牌上限不能冒充原作规则。
- 新局随机种子、手动种子可复现；地图必须连通且具有分叉汇合；战斗抽牌来自当前牌组，不能暗中操纵玩家输赢。
- 界面采用无畏契约技能和战术语境，使用“布防／压制”；技能与选手的搭配是本作设定，真实名场面需要核实来源。
- 测试中的强制胜利仅验证流程，不能作为平衡或实际通关证据。tests/fixtures 是冻结的真实引擎回放；不得随意更新预期结果来掩盖回归。
- 最新策划目标：至少75张不同卡牌构成一个完整可选牌池，不能用四赛区分散总数、升级版或重复副本凑数；12张仅可作为内部机制验证，不能替代用户要求的完整内容目标。战术安排与有规则的随机性为核心。用户希望保留与无畏契约玩家的联系；不等于已确定使用具体官方IP或放弃原创。新战术规则已经由用户授权实现为新demo，但不得擅自替换线上版本；线上旧入口保持不变。
- 用户要求保留当前Demo作为一次尝试。冻结提交461e7d0948c8b478b8203b484466ce79ddc290a2，标签demo-d0.2.0-attempt-2026-09-22，不移动或删除该标签。当前分支 codex/tactical-card-design 提供两个demo入口：本地4180端口提供 /（瓦demo）、/pvp/、/new/；最新用户授权：瓦demo与好友PvP验收后发布main并上线；新demo保留本地4180/new/，生产强制关闭。
- 已确认模式关系：爬塔负责构筑，大关构筑保存到跨局历史库，每人共10个位置（所有局、所有幕共享）。满位由玩家主动选择并确认替换，或放弃保存新构筑，不自动淘汰。PvP是独立真人模式，从库中选同幕卡组对战，首期房间码邀请好友。新开局、失败、放弃、通关或PvP输赢不得自动覆盖历史库。方案见docs/STAGE-SNAPSHOT-PVP.md；回合细则已经实现并验证。
- PvP血量规则已确认：大关奖励结算后按各自最大血量回满再保存；最大血量是成长与构筑成果，完整继承，竞技每场以各自上限满血开始，禁止统一上限或自动拉平。金钱保留但无对战内消费渠道，不转换为战斗加成。旧Demo冻结历史版本30%回血，新版 wa-1 幕奖励后回满。
- 工程评估先参考用户已有噜噜卡（lulu_card）与开瓦包（Val_Manager卡牌模式）的身份、数据库、好友码、服务端操作结算与并发代码，见docs/PEER-PVP-REUSE.md。当前Demo静态/localStorage是版本现状，不是团队能力上限；已经实现好友真人双人房间出牌。
- 服务端重放核验真实构筑：客户端提交完整 action 序列，服务端用同版本引擎重建运行并提取快照，防止伪造牌组或属性；同幕兼容房间码，双方手动出牌，服务端随机决定先手并隐藏对手手牌。
- 匿名持有凭证恢复账号：用户持有 256 位随机 token，可随时恢复账号，服务端仅存哈希；不实现完整实名登录、公共匹配或反作弊平台。
- 新demo存档使用 localStorage 独立存储，不与瓦demo共享；本轮新demo不做 PvP，只做单人爬塔。文件存储已实测，PG适配尚未连接实库验收；Railway发布使用/data持久卷单实例；生产环境必须提供 DATABASE_URL 或挂载持久 volume DATA_DIR 单实例，否则重部署会丢失数据。
